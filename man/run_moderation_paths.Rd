% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_moderation_paths.R
\name{run_moderation_paths}
\alias{run_moderation_paths}
\title{Iterate moderation analyses across variable sets}
\usage{
run_moderation_paths(
  data,
  predictors,
  moderators,
  outcomes,
  controls = NULL,
  categorical_vars = NULL,
  sig_level = 0.05,
  plot_sig = FALSE,
  summarize_categorical = FALSE
)
}
\arguments{
\item{data}{A data frame containing all variables referenced.}

\item{predictors}{Character vector of focal predictor variable names.}

\item{moderators}{Character vector of moderator variable names.}

\item{outcomes}{Character vector of outcome variable names.}

\item{controls}{Optional character vector of control variable names.
Any controls that duplicate the current predictor or moderator are dropped per model.}

\item{categorical_vars}{Optional character vector of variables to coerce to factor
(use this to explicitly mark categorical inputs instead of guessing).}

\item{sig_level}{Numeric p-value threshold used to flag significance (default 0.05).}

\item{plot_sig}{Logical; if \code{TRUE}, attempt to plot significant interactions via
\pkg{interactions}. Plots are best-effort and skipped if the package is unavailable.}

\item{summarize_categorical}{Logical; when the interaction expands to multiple terms
(e.g., factor-by-numeric), record only the most significant interaction term per model.}
}
\value{
A tibble with one row per interaction term (or one per model if
  \code{summarize_categorical = TRUE}) and columns:
  \itemize{
    \item \code{Predictor}, \code{Moderator}, \code{Outcome}, \code{Term}
    \item \code{Interaction_Effect}, \code{Std_Error}, \code{T_value}, \code{P_value}
    \item \code{CI_Lower}, \code{CI_Upper}
    \item \code{Has_Moderation} (logical; \code{P_value < sig_level})
  }
}
\description{
Fits linear models of the form \code{outcome ~ predictor * moderator + controls}
for all combinations of \code{predictors}, \code{moderators}, and \code{outcomes}.
Returns a tidy summary of interaction terms; optionally plots significant effects.
}
\details{
The formula is assembled with backticks to tolerate non-syntactic names.
Models are fit via \code{stats::lm()} with \code{na.action = stats::na.exclude}.
For plotting, \pkg{interactions} is required; if not installed or an error occurs,
plotting is skipped with a message.
}
\examples{
\donttest{
set.seed(1)
df <- data.frame(
  y = rnorm(80),
  x = rnorm(80),
  m = rnorm(80),
  c1 = rnorm(80)
)
res <- run_moderation_paths(
  data = df,
  predictors = "x",
  moderators = "m",
  outcomes = "y",
  controls = "c1",
  plot_sig = FALSE
)
head(res)
}

}
